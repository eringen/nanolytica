package templates

// Dashboard renders the main analytics dashboard page
templ Dashboard(sites []string, currentSite string) {
	@Layout("Nanolytica Dashboard", "visitors", sites, currentSite) {
		@PeriodSelector("week")
		@ContentContainer()
	}
}
