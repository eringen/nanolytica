package templates

// Dashboard renders the main analytics dashboard page
templ Dashboard(sites []string, currentSite string) {
	@Layout("Nanolytica Dashboard", "visitors") {
		@SiteSelector(sites, currentSite)
		@PeriodSelector("week")
		@ContentContainer()
	}
}
